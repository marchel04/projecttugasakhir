# ========================================
# TEMPLATE ENV UNTUK VPS PRODUCTION
# ========================================
# Copy file ini ke .env dan update nilai sesuai VPS Anda

# -------- DATABASE CONFIGURATION --------
# Format: mysql://username:password@host:port/database_name
# 
# PENTING: Sesuaikan dengan:
# - username MySQL Anda
# - password MySQL Anda  
# - host (IP atau hostname database server)
# - port (default 3306)
# - database name (default: absensi)
#
# Contoh 1: Database di server yang sama dengan backend
# DATABASE_URL="mysql://root:MyPassword123@127.0.0.1:3306/absensi"
#
# Contoh 2: Database di server terpisah
# DATABASE_URL="mysql://user:password@192.168.1.100:3306/absensi"
#
DATABASE_URL="mysql://username:password@host:3306/absensi"

# -------- SECURITY --------
# JWT Secret key - HARUS minimal 32 karakter random dan unik!
# Generate menggunakan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secure-random-key-minimum-32-characters-long"

# -------- ENVIRONMENT --------
# Jangan ubah ke "development" saat production
APP_ENV="production"

# -------- SERVER PORT --------
# Port tempat backend berjalan
PORT=4000

# -------- FRONTEND ORIGIN (CORS) --------
# URL frontend yang diizinkan mengakses backend
# 
# Contoh 1: Menggunakan IP VPS
# ORIGIN="http://185.123.123.123:3000"
#
# Contoh 2: Menggunakan domain
# ORIGIN="https://absensi.company.com"
#
# PENTING: Pastikan sesuai dengan URL yang digunakan user untuk akses
ORIGIN="http://your-vps-ip:3000"

# ========================================
# OPTIONAL CONFIGURATIONS
# ========================================

# Email Configuration (jika ada fitur email notification)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"

# Storage Configuration
# STORAGE_PATH="./storage"
# MAX_FILE_SIZE=5242880  # 5MB

# Session Configuration  
# SESSION_SECRET="another-random-secret-key"
# SESSION_TIMEOUT=3600  # 1 hour in seconds
